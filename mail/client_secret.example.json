{
  /*
   * Google OAuth 2.0 Client Secrets Example File
   * --------------------------------------------
   * This file is required to authenticate your application with Google APIs,
   * such as Gmail, Google Drive, or other Google services.
   *
   * 📌 How to Obtain This File:
   * 1. Go to Google Cloud Console: https://console.cloud.google.com/
   * 2. Create a new project or select an existing one.
   * 3. Navigate to "APIs & Services" → "Credentials".
   * 4. Click "Create Credentials" → "OAuth client ID".
   * 5. Select "Application Type: Desktop App" (or Web App if applicable).
   * 6. Click "Create" and then "Download JSON".
   * 7. Rename this file to `client_secrets.json` and replace the placeholders below.
   *
   * 🚨 Important:
   * - NEVER share this file publicly or commit it to version control (GitHub, etc.).
   * - If leaked, regenerate a new secret in Google Cloud Console.
   * - Use environment variables or a secrets manager for security in production.
   */

  "installed": {
    "client_id": "insert_client_id",  // Your OAuth client ID from Google
    "project_id": "insert_project_id",  // Your Google Cloud project ID
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "insert_client_secret",  // Your OAuth client secret (keep it private!)
    "redirect_uris": [
      "http://localhost"  // The redirect URI for local development authentication
    ]
  }
}
